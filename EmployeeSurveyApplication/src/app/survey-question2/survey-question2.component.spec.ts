import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { SurveyQuestion2Component } from './survey-question2.component';

/*
  describe('SurveyQuestion2Component', () => {
    let component: SurveyQuestion2Component;
    let fixture: ComponentFixture<SurveyQuestion2Component>;
  
    beforeEach(async () => {
      await TestBed.configureTestingModule({
        imports: [],
        declarations: [ SurveyQuestion2Component ],
        providers: []
      })
      .compileComponents().then(() => {
        fixture = TestBed.createComponent(SurveyQuestion2Component);
        component = fixture.componentInstance;
      });
    });
  
    beforeEach(() => {
      TestBed.configureTestingModule({
         declarations: [SurveyQuestion2Component]
      });
  
      fixture = TestBed.createComponent(SurveyQuestion2Component);
      component = fixture.componentInstance;
      //fixture.detectChanges();
    });
  });
  */