import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { SurveyQuestion4Component } from './survey-question4.component';

/*
  describe('SurveyQuestion4Component', () => {
    let component: SurveyQuestion4Component;
    let fixture: ComponentFixture<SurveyQuestion4Component>;
  
    beforeEach(async () => {
      await TestBed.configureTestingModule({
        imports: [],
        declarations: [ SurveyQuestion4Component ],
        providers: []
      })
      .compileComponents().then(() => {
        fixture = TestBed.createComponent(SurveyQuestion4Component);
        component = fixture.componentInstance;
      });
    });
  
    beforeEach(() => {
      TestBed.configureTestingModule({
         declarations: [SurveyQuestion4Component]
      });
  
      fixture = TestBed.createComponent(SurveyQuestion4Component);
      component = fixture.componentInstance;
      //fixture.detectChanges();
    });
  });
  */