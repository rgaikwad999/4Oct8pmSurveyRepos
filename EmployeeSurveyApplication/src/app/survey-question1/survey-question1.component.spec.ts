import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { SurveyQuestion1Component } from './survey-question1.component';

/*
describe('SurveyHomeComponent', () => {
  let component: SurveyQuestion1Component;
  let fixture: ComponentFixture<SurveyQuestion1Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [],
      declarations: [ SurveyQuestion1Component ],
      providers: []
    })
    .compileComponents().then(() => {
      fixture = TestBed.createComponent(SurveyQuestion1Component);
      component = fixture.componentInstance;
    });
  });

  beforeEach(() => {
    TestBed.configureTestingModule({
       declarations: [SurveyQuestion1Component]
    });

    fixture = TestBed.createComponent(SurveyQuestion1Component);
    component = fixture.componentInstance;
    //fixture.detectChanges();
  });


  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/