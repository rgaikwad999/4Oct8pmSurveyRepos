import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { SurveyQuestion3Component } from './survey-question3.component';

/*
  describe('SurveyQuestion3Component', () => {
    let component: SurveyQuestion3Component;
    let fixture: ComponentFixture<SurveyQuestion3Component>;
  
    beforeEach(async () => {
      await TestBed.configureTestingModule({
        imports: [],
        declarations: [ SurveyQuestion3Component ],
        providers: []
      })
      .compileComponents().then(() => {
        fixture = TestBed.createComponent(SurveyQuestion3Component);
        component = fixture.componentInstance;
      });
    });
  
    beforeEach(() => {
      TestBed.configureTestingModule({
         declarations: [SurveyQuestion3Component]
      });
  
      fixture = TestBed.createComponent(SurveyQuestion3Component);
      component = fixture.componentInstance;
      //fixture.detectChanges();
    });
  });
  */